# Environment configuration for Life RPG
# Database connection string (SQLite file path, zero-docker required)
DATABASE_URL="file:./dev.db"

# JWT Secret for user session tokens
JWT_SECRET="life-rpg-mystic-secret-token-key-super-secure-change-in-prod-2025"

# Application Base URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
